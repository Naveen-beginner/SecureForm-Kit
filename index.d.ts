export function emailValidator(email: string): {
    success: boolean
    message: string
    data?: string
  }
  
  export function passwordValidator(password: string): {
    success: boolean
    message: string
    data?: string
  }